spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/oneDayDB
    username: postgres
    password: 1472
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080

